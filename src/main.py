#initialize the game, load necessary assets, and call functions to run the game loop.
from cmu_graphics import *

WIDTH, HEIGHT = 1200, 1000
app = App(width=WIDTH, height=HEIGHT)

def main():
    # main game loop?
    app.run()